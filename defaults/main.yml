---
# defaults file for the role
git_package_name: git
pip_package_name: python3-pip

ansible_package_name: ansible
ansible_package_state: present # present, absent, latest
ansible_package_manage: true
